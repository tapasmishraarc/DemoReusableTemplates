name: Main

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build_dotnet:
    uses: tapasmishraarc/demo-action-composite/.github/workflows/build.yml@main
    with:
      dotnet-version: 5.0.x
      project-path: src/DemoReusableTemplates.csproj
        
